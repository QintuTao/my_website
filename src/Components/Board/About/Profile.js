import React from 'react';
import Title from '../../Title/Title';
import './Profile.scss';

const Profile = (props) => {
    return (
        <div className="profile">
            <Title field="about" />

        </div>
    )
}

export default Profile;